{
  "name": "ai-orchestration",
  "version": "1.0.0",
  "description": "Desafío 5 (LLD): orquestación de agentes IA con Command + State + Chain of Responsibility, guardrails y Human-in-the-Loop",
  "type": "module",
  "scripts": {
    "start": "tsx src/main.ts",
    "test": "tsx --test tests/*.test.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "keywords": ["ai-agents", "command", "state", "chain-of-responsibility", "guardrails", "solid", "lld"],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.20.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
